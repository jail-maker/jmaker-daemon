'use strict';

const Layers = require('./layers.js');
module.exports = Layers;

// let layers = new Layers('jmaker');
// let layer = layers.get('second');

// console.log(layer);
// layer.compress();
